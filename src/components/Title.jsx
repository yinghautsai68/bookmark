import React from 'react'

const Title = ({ text }) => {
    return (
        <div>
            <h1 className='font-imprima text-[40px] text-center  '>{text}</h1>
        </div>
    )
}

export default Title